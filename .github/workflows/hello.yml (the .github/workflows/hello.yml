name: Hello Action with Email

on: [push]  # Triggers on any push to repo

jobs:
  test:
    runs-on: ubuntu-latest  # Free GitHub runner (kitchen station)
    steps:
      - name: Checkout code  # Step 1: Grab your repo files
        uses: actions/checkout@v4
      
      - name: 23f2005404@ds.study.iitm.ac.in  # Your required step (Step 2)
        run: echo "Hello from NITIN DIXIT's action!"
      
      - name: Final check  # Step 3: Confirm it ran
        run: echo "Action complete! Most recent workflow succeeded."
